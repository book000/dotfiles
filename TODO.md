# TODO

- `chezmoi` の age 暗号化を本運用に移行する（鍵生成、`key.txt.age` の用意、テンプレートの暗号化）。
- `.github/copilot-instructions.md` などルートのプロンプトがガイドラインに沿っているか再確認する。
- `home/` 配下の汎用プロンプト（`home/dot_codex/AGENTS.md` / `home/dot_gemini/GEMINI.md` / `home/dot_copilot/copilot-instructions.md`）を残すか削除するか決める。
- 実機環境で `chezmoi apply` を実行して最終確認する。
